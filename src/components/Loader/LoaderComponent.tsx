import {InfinitySpin} from 'react-loader-spinner';

const LoaderComponent: React.FC = () => {
    return (
        <InfinitySpin type="Oval" color="#00BFFF" height={50} width={50} />
    );
};

export default LoaderComponent;
